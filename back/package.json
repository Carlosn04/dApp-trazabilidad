{
  "scripts": {
    "compile": "npx hardhat compile",
    "format": "node format-contracts.js",
    "node": "npx hardhat node",
    "deploy": "npx hardhat run scripts/deploy.js --network localhost",
    "copy": "cp smart-contracts.json ../front/smart-contracts.json",
    "init": "npm install && npm run node",
    "start": "npm run compile && npm run format && npm run deploy && npm run copy"
  },
  "devDependencies": {
    "hardhat": "^2.17.4"
  },
  "dependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "ethers": "^6.7.1"
  }
}
